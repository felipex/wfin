{% extends "base.html" %}

{% block content %}


<h3>.</h3>
<div class="col-sm-2"></div>
<div class="panel panel-primary col-sm-8">
  <!-- Default panel contents -->
  
  <div class="panel-heading">Aluno </div>
  <div class="panel-body">
<form action="" method="POST">
{{ form.csrf_token }}
<input type="hidden" name="id" value="{{obj.id}}">

<div class="form-group">
<label>{{ form.nome.label }}</label> {{ form.nome(size=50, placeholder="Nome do aluno",  value=obj.nome, class="form-control") }}
</div>

<div class="form-group">
<label>{{ form.responsavel.label }}</label> {{ form.responsavel(size=50, value=obj.responsavel, class="form-control") }}
</div>

<div class="form-group">
<label>{{ form.parentesco.label }}</label> {{ form.parentesco(size=50, value=obj.parentesco, class="form-control") }}
</div>

<div class="form-group">
<label>{{ form.serie.label }}</label> {{ form.serie(size=50, value=obj.serie, class="form-control") }}
</div>

<button type="submit" class="btn btn-primary">Gravar </button>
<button type="button" class="btn btn-danger" onclick="javascript:window.location='/responsavel/list'">Cancela </button>
</form>


</div>
</div>

{% endblock %}

