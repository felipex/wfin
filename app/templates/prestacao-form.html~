{% extends "base.html" %}

{% block extrahead %}

{% endblock %}


{% block content %}


<p></p>
<div class="col-sm-1"></div>
<div class="panel panel-primary col-sm-10">
  <!-- Default panel contents -->
  <div class="panel-heading"><strong>Prestação</strong> </div>
  <div class="panel-body">

<form action="" method="POST">
{{ form.csrf_token }}
<input type="hidden" name="id" value="{{obj.id}}">
<div class="row">
    <div class="col-sm-3">
        <div class="form-group">
        <label>{{ form.mes.label }}</label> <h4 style="background-color: gray; color:white; padding: 2mm; margin-top:0; text-align: center"> <strong> {{obj.messtr()}}</strong></h4>
        </div>
    </div>
    <div class="col-sm-3">
        <div class="form-group">
        <label>{{ form.vencimento.label }}</label> {{form.vencimento(size=15, value=obj.vencimento, class="form-control")}}
        <!--
<div class="input-group date">
  <input type="text" class="form-control"><span class="input-group-addon"><i class="glyphicon glyphicon-th"></i></span>
</div>
-->
        </div>
    </div>


    <div class="col-sm-2">
        <div class="form-group">
        <label>{{ form.valor.label }}</label> {{ form.valor(size=15, value=obj.valor, class="form-control") }}
        </div>
    </div>


</div>

<div class="row">
    <div class="col-sm-3">
        <div class="form-group">
         <label>Situação</label>
            <select class="form-control" id="status" name="status" >
                <option value="{{obj.status}}" {% if obj.status=='A' %}selected{% endif %}>Aberta</option>
                <option value="{{obj.status}}" {% if obj.status=='P' %}selected{% endif %}>Paga</option>
                <option value="{{obj.status}}" {% if obj.status=='D' %}selected{% endif %}>Desistente</option>
                <option value="{{obj.status}}" {% if obj.status=='N' %}selected{% endif %}>Não estudou</option>
           
            </select>          
        </div>
    </div>
    
    <div class="col-sm-3">
        <div class="form-group">
        <label>{{ form.data_pgto.label }}</label> {{ form.data_pgto(size=15, value=obj.data_pgto, class="form-control") }}
        </div>
    </div>
    
    <div class="col-sm-2">
        <div class="form-group">
        <label>{{ form.valor_pago.label }}</label> {{ form.valor_pago(size=15, value=obj.valor_pago, class="form-control") }}
        </div>
    </div>
    
    
</div>

<div class="row">
    <div class="col-sm-3">
        <div class="form-group">
         <label>Forma de pgto</label>
            <select class="form-control" id="forma_pgto" name="forma_pgto" >
                <option value="{{obj.forma_de_pgto}}" {% if obj.status=='E' %}selected{% endif %}>Espécie</option>
                <option value="{{obj.forma_de_pgto}}" {% if obj.status=='C' %}selected{% endif %}>Cheque</option>
           
            </select>          
        </div>
    </div>
    
    <div class="col-sm-3">
        <div class="form-group">
        <label>{{ form.cheque_numero.label }}</label> {{ form.cheque_numero(size=15, value=obj.cheque_numero, class="form-control") }}
        </div>
    </div>
    
    <div class="col-sm-2">
        <div class="form-group">
        <label>{{ form.cheque_data.label }}</label> {{ form.cheque_data(size=15, value=obj.cheque_data, class="form-control") }}
        </div>
    </div>
        
</div>

<button type="submit" class="btn btn-primary">Gravar </button>
<button type="button" class="btn btn-danger" onclick="javascript:window.location='/prestacao/list/{{obj.contrato_id}}'">Cancela </button>
</form>


</div>
</div>

{% endblock %}

{% block final %}
<script>
$('.input-group.date').datepicker({
    language: "pt-BR",
    autoclose: true,
    todayHighlight: true});
</script>
{% endblock %}


