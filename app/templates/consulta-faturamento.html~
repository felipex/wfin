{% extends "base_site.html" %}

{% block title %} Dashboard 1 {% endblock title %}

{% block stylesheets %}
  {{ super() }}
 <style>
 td, th {
    text-align: right;
 }
 
 td.total {
    font-weight: bold;
 }
 </style>
{% endblock stylesheets %}

{% block content %}

<div class="col-lg-9" style="margin-left:250px; min-height: 550px;">
  <!-- Default panel contents -->
  
  <h3>Faturamento até {{ data.strftime("%d-%m-%Y") }} </h3>
  <div class="">
  
<br />
    
  <!-- Table -->
  <table class="table table-striped" >
  <tr ><th></th><th>Jan</th><th>Fev</th><th>Mar</th><th>Abr</th><th>Mai</th><th>Jun</th>
                    <th>Jul</th><th>Ago</th><th>Set</th><th>Out</th><th>Nov</th><th>Dez</th><th>Total</th></tr>

<tr><td style="vertical-align: middle;"><strong>Previsão</strong></td>
        {% for p in previsao %}
<td style="vertical-align: middle;">
{{formatfloat(p)}}
</td>
        {% endfor %}
<td class="total">{{ formatfloat(total['previsao']) }}</td>
</tr>
<tr><td style="vertical-align: middle;"><strong>Recebido</strong></td>
        {% for p in recebido %}
<td style="vertical-align: middle;">
{{formatfloat(p)}}
</td>
        {% endfor %}
<td class="total">{{ formatfloat(total['recebido']) }}</td>
</tr>

<tr><td style="vertical-align: middle;"><strong>Atrasado</strong></td>
        {% for p in em_atraso %}
<td style="vertical-align: middle;">
{{formatfloat(p)}}
</td>
        {% endfor %}
<td class="total">{{ formatfloat(total['em_atraso']) }}</td>
</tr>

  </table>
</div>
</div>

{% endblock %}

